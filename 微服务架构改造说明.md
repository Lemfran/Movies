# 影评系统微服务架构改造说明

## 一、实验要求与实现对应

| 序号 | 实验要求 | 实现技术 | 关键文件 |
|------|---------|---------|---------|
| 1 | 用Java语言实现简单的Java Web服务 | Spring Boot 3.4.1 + Java 17 | `Movies/pom.xml` |
| 2 | 配置服务注册与发现 | Eureka Server + Eureka Client | `eureka-server/`, `@EnableDiscoveryClient` |
| 3 | 通过RPC框架调用远程方法 | OpenFeign + `@FeignClient` | `client/*.java`, `*RpcController.java` |
| 4 | 配置并使用分布式配置中心 | Spring Cloud Config Server + Client | `config-server/`, `config-repo/`, `bootstrap.yml` |
| 5 | 配置并使用微服务网关 | Spring Cloud Gateway | `gateway-service/` |
| 6 | 配置容错处理与负载均衡 | Resilience4j + Spring Cloud LoadBalancer | `MovieClientService.java`, 多实例部署 |
| 7 | 实现微服务中统一认证与授权 | JWT Token + Gateway全局过滤器 | `JwtUtil.java`, `AuthFilter.java`, `AuthController.java` |
| 8 | 实现微服务架构的Web系统 | 6个微服务协作的完整系统 | 见下方架构图 |

---

## 二、技术替代说明

由于项目使用 **Spring Cloud 2024.0.0**，以下组件已被官方移除，采用等效的现代替代方案：

| 原组件 | 状态 | 替代方案 | 说明 |
|--------|------|---------|------|
| Zuul | 已弃用 | Spring Cloud Gateway | 等效的路由转发 + 过滤器链功能 |
| Ribbon | 已弃用 | Spring Cloud LoadBalancer | 等效的客户端负载均衡功能 |
| Hystrix | 已停止维护 | Resilience4j | 新一代容错框架 |

---

## 三、系统架构

```
                          用户请求
                             |
                             v
                   +---------------------+
                   |  Gateway (端口8080) |   <-- 统一入口
                   |   Spring Cloud      |       JWT认证过滤器 (pre)
                   |   Gateway           |       请求日志过滤器 (post)
                   +----------+----------+
                              |
           +------------------+------------------+------------------+
           |                  |                  |                  |
           v                  v                  v                  v
   +---------------+  +-------------------+  +-------------------+  +---------------+
   | movies-service|  | user-portal-service|  | recommendation-svc|  | config-server |
   |   (端口8081)  |  |   (端口8082)       |  |    (端口8083)     |  |  (端口8888)   |
   |   (端口8084)  |  |   (端口8085)       |  |                   |  |               |
   |               |  |                    |  |                   |  |               |
   | MyBatis+MySQL |  | OpenFeign客户端    |  |  Resilience4j     |  | 外部化配置     |
   | OpenFeign RPC |  | Thymeleaf页面      |  |  熔断/重试/降级   |  |               |
   | 管理员后台     |  | 用户端门户         |  |                   |  |               |
   +---------------+  +-------------------+  +-------------------+  +---------------+
           |                  |                  |
           +------------------+------------------+
                              |
                              v
                    +-----------------+
                    | Eureka Server   |   <-- 服务注册中心
                    |   (端口8761)    |
                    +-----------------+
                              |
                              v
                    +-----------------+
                    |   Redis Server  |   <-- Session共享存储
                    |   (端口6379)    |
                    +-----------------+
```

### 核心服务职责分离

| 服务 | 职责 |
|------|------|
| **movies-service** (核心服务) | 数据库访问、管理员后台页面、REST API、RPC接口、JWT认证 |
| **user-portal-service** (用户门户) | 用户端Thymeleaf页面、通过OpenFeign调用movies-service |
| **recommendation-service** (推荐服务) | 热门推荐、Resilience4j熔断降级演示 |
| **gateway-service** (API网关) | 统一入口、JWT认证、路由转发 |
| **eureka-server** (注册中心) | 服务注册与发现 |
| **config-server** (配置中心) | 外部化配置管理 |

---

## 四、各服务端口一览

| 服务 | 端口 | 说明 |
|------|------|------|
| Redis | 6379 | Session 共享存储 |
| eureka-server | 8761 | 服务注册中心控制台 |
| config-server | 8888 | 分布式配置中心 |
| movies-service | 8081 / 8084 | 核心业务服务（双实例负载均衡） |
| user-portal-service | 8082 / 8085 | 用户门户服务（双实例负载均衡） |
| recommendation-service | 8083 | 推荐服务 |
| gateway-service | 8080 | API网关（统一入口） |

**多实例部署说明**：
- `movies-service` 部署 2 个实例（8081、8084），Gateway 通过 `lb://movies-service` 轮询分发请求
- `user-portal-service` 部署 2 个实例（8082、8085），Gateway 通过 `lb://user-portal-service` 轮询分发请求
- 其余服务为单实例部署

---

## 五、微服务网关（Spring Cloud Gateway）

### 路由规则

```yaml
# Gateway 路由配置 (application.yml)
/              -->  RedirectTo /user/login              # 根路径重定向到用户登录
/api/**        -->  lb://movies-service                # 电影REST API
/rpc/**        -->  lb://movies-service                # 内部RPC接口
/auth/**       -->  lb://movies-service                # JWT认证接口（白名单）
/admin/**      -->  lb://movies-service (StripPrefix=1) # 管理员后台页面
/user/**       -->  lb://user-portal-service           # 用户门户页面
/uploads/**    -->  lb://user-portal-service           # 海报文件上传访问
/recommend/**  -->  lb://recommendation-service        # 推荐服务
```

### 过滤器链

| 过滤器 | 类型 | 顺序 | 功能 |
|--------|------|------|------|
| `LoggingFilter` | GlobalFilter (post) | order=0 | 记录所有请求的方法、URI、IP、耗时 |
| `AuthFilter` | GlobalFilter (pre) | order=1 | JWT验证、白名单放行、用户信息注入 |

### JWT 认证过滤器流程

1. 请求到达 Gateway (8080)
2. `LoggingFilter` 记录请求日志
3. `AuthFilter` 检查路径是否在白名单
   - 页面端点白名单：`/user/`, `/admin/`, `/recommend/`, `/uploads/`
   - API端点白名单：`/auth/login`, `/auth/register`, `/eureka`, `/actuator`, `/api/info`, `/rpc/movie/info`
   - 白名单路径直接放行
4. 非白名单路径：从 Header (`Authorization: Bearer <token>`) 或 Cookie 提取 JWT
5. 验证 Token 有效性
   - 无效/过期 → 返回 401 Unauthorized
   - 有效 → 将 `X-User-Id`, `X-Username` 注入请求头 → 转发给下游服务

### 管理员端与用户端分离

```
访问路径:
  http://localhost:8080/user/...   → 用户端门户 (user-portal-service)
  http://localhost:8080/admin/...  → 管理员后台 (movies-service)
  http://localhost:8080/api/...    → REST API (movies-service)
  http://localhost:8080/recommend/... → 推荐服务 (recommendation-service)
```

---

## 六、统一认证与授权（JWT）

### 认证流程

```
用户                    Gateway                    movies-service
 |                        |                              |
 |---POST /auth/login---->|                              |
 |  username+password     |                              |
 |                        |---转发到 /auth/login-------->|
 |                        |                              |---查询数据库验证
 |                        |                              |---生成JWT Token
 |                        |<---返回 {token, ...}---------|
 |<---返回JWT Token-------|                              |
 |                        |                              |
 |---后续请求携带Token---->|                              |
 |  Authorization: Bearer |                              |
 |                        |---验证Token有效-------------|
 |                        |---注入X-User-Id/X-Username-->|
 |                        |                              |---执行业务逻辑
```

### 新增接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/auth/login` | POST | 用户名+密码登录，返回JWT Token |
| `/auth/register` | POST | 注册新用户，返回JWT Token |
| `/auth/verify` | GET | 验证Token有效性 |

### 关键代码

- `movies-service/src/main/java/edu/cuit/yingpingsxitong/util/JwtUtil.java` — Token 生成/验证
- `movies-service/src/main/java/edu/cuit/yingpingsxitong/Controller/AuthController.java` — 认证接口
- `movies-service/src/main/java/edu/cuit/yingpingsxitong/config/SecurityConfig.java` — Security配置
- `gateway-service/src/main/java/edu/cuit/gateway/filter/AuthFilter.java` — 网关统一拦截

> **注意**：管理员端 (`/admin/**`) 和用户端 (`/user/**`) 均使用基于 Session 的登录认证，通过 Gateway 路由到各自的服务。JWT 认证主要用于 API 接口 (`/api/**`, `/rpc/**`) 的统一认证。

---

## 七、用户门户服务（user-portal-service）

### 架构定位

`user-portal-service` 是用户端门户，作为 `movies-service` 的数据消费者，通过 **OpenFeign** 调用后者的 RPC 接口获取数据，自身不直接访问数据库。

### Feign 客户端

| 客户端 | 调用服务 | 功能 |
|--------|---------|------|
| `MovieClient` | movies-service `/rpc/movie/**` | 电影列表、搜索、详情 |
| `UserClient` | movies-service `/rpc/user/**` | 用户注册、登录、信息 |
| `ReviewClient` | movies-service `/rpc/review/**` | 评论查看、提交 |

```java
@FeignClient(name = "movies-service")
public interface MovieClient {
    @GetMapping("/rpc/movie/findAll")
    Result<List<Movie>> findAllMovies();
    // ...
}
```

### 页面列表

| 页面 | 路径 | 说明 |
|------|------|------|
| 首页 | `/user/index` | 电影展示首页 |
| 电影列表 | `/user/movielist` | 全部电影列表 |
| 电影详情 | `/user/getmovie` | 电影详情 + 评论 |
| 添加评论 | `/user/addreview` | 提交电影评论 |
| 评论成功 | `/user/reviewsuccess` | 评论提交成功页 |
| 注册 | `/user/register` | 用户注册 |
| 注册成功 | `/user/zcsuccess` | 注册成功页 |
| 登录 | `/user/login` | 用户登录 |

---

## 八、容错处理（Resilience4j）

### 设计要点：避免同类自调用

**重要**：Spring AOP 代理只能拦截外部方法调用，**不能拦截同类方法之间的自调用**。因此将 HTTP 调用从 `RecommendationService` 抽离到独立的 `MovieClientService` 中：

```java
@Service
public class MovieClientService {

    @CircuitBreaker(name = "movies-service", fallbackMethod = "fetchAllMoviesFallback")
    @Retry(name = "movies-service")
    public List<Map<String, Object>> fetchAllMovies() {
        // 调用 movies-service /api/movies
    }

    public List<Map<String, Object>> fetchAllMoviesFallback(Exception ex) {
        // 降级：返回 "服务暂时不可用" 提示数据
    }
}
```

```java
@Service
public class RecommendationService {
    @Autowired
    private MovieClientService movieClientService;  // 注入外部服务，AOP可拦截

    public List<Map<String, Object>> getHotRecommendations(int limit) {
        return movieClientService.fetchAllMovies().stream()...;
    }
}
```

### 配置参数

```properties
# 熔断器配置
resilience4j.circuitbreaker.instances.movies-service.registerHealthIndicator=true
resilience4j.circuitbreaker.instances.movies-service.slidingWindowSize=5
resilience4j.circuitbreaker.instances.movies-service.permittedNumberOfCallsInHalfOpenState=3
resilience4j.circuitbreaker.instances.movies-service.waitDurationInOpenState=10s
resilience4j.circuitbreaker.instances.movies-service.failureRateThreshold=50

# 重试配置
resilience4j.retry.instances.movies-service.maxAttempts=3
resilience4j.retry.instances.movies-service.waitDuration=1s
```

### 效果验证

```
[熔断降级] movies-service 调用失败，返回降级数据。原因: I/O error on GET request...
[熔断降级] movies-service 调用失败，返回降级数据。原因: I/O error on GET request...
[熔断降级] movies-service 调用失败，返回降级数据。原因: CircuitBreaker 'movies-service' is OPEN and does not permit further calls
```

- 失败率超过 50% 时，熔断器 OPEN
- 10 秒内所有请求直接走降级方法
- 10 秒后进入半开状态，尝试恢复

---

## 九、负载均衡（LoadBalancer）

### Spring Cloud LoadBalancer（Ribbon 的替代方案）

Gateway 和 OpenFeign 均内置 Spring Cloud LoadBalancer，自动从 Eureka 获取服务实例列表并进行负载分配：

| 调用方 | 被调用方 | 负载均衡方式 |
|--------|---------|-------------|
| Gateway | movies-service | `lb://movies-service` 轮询到 8081/8084 |
| Gateway | user-portal-service | `lb://user-portal-service` 轮询到 8082/8085 |
| user-portal-service (Feign) | movies-service | Feign内置LoadBalancer轮询到 8081/8084 |
| recommendation-service | movies-service | `@LoadBalanced RestTemplate` 轮询 |

### 多实例部署验证

Eureka 控制台显示：
- `MOVIES-SERVICE`：2 个实例 (8081, 8084)
- `USER-PORTAL-SERVICE`：2 个实例 (8082, 8085)
- `RECOMMENDATION-SERVICE`：1 个实例 (8083)
- `GATEWAY-SERVICE`：1 个实例 (8080)

---

## 十、多实例 Session 共享（Spring Session Redis）

### 问题背景

`movies-service`（8081/8084）和 `user-portal-service`（8082/8085）均部署了多实例。当用户在 8082 登录后，Session 保存在 8082 的内存中；下一次请求被 Gateway 轮询到 8085 时，8085 没有该 Session，导致显示"未登录"。

### 解决方案

引入 **Spring Session Redis**，将所有实例的 Session 统一存储到 Redis，实现跨实例共享：

```properties
spring.session.store-type=redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
```

### 依赖

```xml
<dependency>
    <groupId>org.springframework.session</groupId>
    <artifactId>spring-session-data-redis</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
```

### 效果

- 用户在任意实例登录，Session 写入 Redis
- 所有实例从 Redis 读取同一 Session，登录状态保持一致
- 管理员端 (`/admin/**`) 和用户端 (`/user/**`) 均不再受实例切换影响

---

## 十一、配置中心（Config Server）

### 技术注意

Config Server 使用 `native` 模式（本地文件系统）而非 Git，以兼容含中文字符的路径：

```properties
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=file:///Users/fanjinchen/工程实践/Movies/Movies/config-repo
```

同时 `movies-service` 的 `application.properties` 中保留了数据库等核心配置的本地 fallback，确保 Config Server 不可用时服务仍能启动。

---

## 十一、管理员端模板组件化

### adminfragments.html

管理员端所有页面统一引用 `adminfragments.html` 中的片段：

| 片段 | 用途 |
|------|------|
| `sidebar(activeMenu)` | 侧边栏导航，高亮当前菜单项 |
| `userBarScript` | 底部用户栏展开/收起交互 |

### 使用方式

```html
<aside th:replace="~{adminfragments :: sidebar('users')}"></aside>
<script th:replace="~{adminfragments :: userBarScript}"></script>
```

### 已组件化的页面

`manager.html`, `usermanage.html`, `moviemanage.html`, `addmovie.html`, `updatemovie.html`, `reviewmanage.html`, `loglist.html`, `updatesuccess.html`

---

## 十二、海报文件上传

### 功能说明

管理员添加/编辑电影时，支持两种方式提供海报：
1. **图片URL**：直接输入网络图片地址
2. **本地上传**：选择本地图片文件上传

### 实现

```java
private static final String UPLOAD_DIR = "/Users/fanjinchen/工程实践/Movies/uploads/posters/";

private String savePosterFile(MultipartFile posterFile) {
    // 生成 UUID 文件名，保存到 uploads/posters/ 目录
}
```

- 表单使用 `enctype="multipart/form-data"`
- 上传文件优先于 URL 输入
- 图片展示使用 `onerror` 回退到占位图

---

## 十三、运行验证结果

所有 6 个服务、8 个实例成功运行，验证结果如下：

| 验证项 | 结果 |
|--------|------|
| 8 个实例全部启动 | ✅ |
| Eureka 注册中心显示所有服务 | ✅ MOVIES-SERVICE(×2), USER-PORTAL-SERVICE(×2), RECOMMENDATION-SERVICE, GATEWAY-SERVICE |
| Gateway 白名单 `/user/` 免认证 | ✅ HTTP 200 |
| Gateway 拦截无 Token 请求 | ✅ HTTP 401 |
| JWT 登录获取 Token | ✅ 返回 token + username + userId |
| 带 Token 访问 `/api/movies` | ✅ 返回电影列表数据 |
| 带 Token 访问 `/recommend/` | ✅ HTTP 200 |
| Resilience4j 熔断降级 | ✅ 降级日志，熔断器 OPEN |
| 用户端通过 Feign 获取数据 | ✅ 电影列表/详情/评论正常显示 |
| 管理员端模板组件化 | ✅ 侧边栏统一 |
| 海报上传功能 | ✅ 文件保存 + 图片回显 |

### 关键 curl 验证命令

```bash
# 1. 登录获取 JWT Token
TOKEN=$(curl -s -X POST "http://localhost:8080/auth/login" \
  -d "username=demo&password=123456" | grep -o '"token":"[^"]*"' | cut -d'"' -f4)

# 2. 无 Token 访问被拦截（应返回 401）
curl -s http://localhost:8080/api/movies
# {"code":401,"message":"缺少认证令牌"}

# 3. 带 Token 访问成功
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/movies

# 4. 白名单路径无需 Token
curl -s http://localhost:8080/user/movielist

# 5. 验证多实例注册
curl -s http://localhost:8761/eureka/apps/MOVIES-SERVICE | grep instanceId
# 应显示 2 个 instanceId
```

---

## 十四、启动与关闭

### 一键启动

```bash
./start.sh
```

启动顺序：Redis (6379) → Eureka (8761) → Config Server (8888) → movies-service (8081, 8084) → user-portal-service (8082, 8085) → recommendation-service (8083) → Gateway (8080)

`start.sh` 会自动检查 Redis，未运行则 `redis-server --daemonize yes` 启动。

### 状态检查

```bash
./status.sh
```

显示 9 个端口的运行状态（Redis + 6 个服务 + 2 个多实例）。

### 关闭服务

```bash
./stop.sh
```

关闭顺序与启动相反，先关 Gateway，最后关 Eureka 和 Redis，支持 graceful shutdown（SIGTERM）和强制终止（SIGKILL）。

---

## 十五、项目目录结构

```
Movies/
|
├── eureka-server/              # 服务注册中心
│   └── src/main/java/edu/cuit/eurekaserver/
│       └── EurekaServerApplication.java
│
├── Movies/                     # 核心服务 (movies-service)
│   ├── config-server/          # 配置中心
│   │   └── src/...
│   ├── config-repo/            # 配置文件仓库
│   │   ├── movies-service.yml
│   │   ├── movies-service-dev.yml
│   │   └── movies-service-test.yml
│   └── src/main/java/edu/cuit/yingpingsxitong/
│       ├── Controller/
│       │   ├── AuthController.java           # JWT认证接口
│       │   ├── IndexController.java          # 登录页
│       │   ├── UserController.java           # 用户管理API
│       │   ├── ManagerController.java        # 管理员后台
│       │   ├── MovieRestController.java      # REST API
│       │   ├── MovieRpcController.java       # RPC接口(供Feign调用)
│       │   ├── UserRpcController.java
│       │   ├── ReviewRpcController.java
│       │   └── LogRpcController.java
│       ├── Service/
│       ├── Dao/
│       ├── Entity/
│       ├── client/             # Feign客户端(调用其他服务)
│       ├── util/
│       │   └── JwtUtil.java    # JWT生成/验证
│       ├── config/
│       │   └── SecurityConfig.java
│       └── YingpingsxitongApplication.java
│
├── user-portal-service/        # 用户门户服务
│   └── src/main/java/edu/cuit/userportal/
│       ├── UserPortalApplication.java        # @EnableFeignClients
│       ├── client/
│       │   ├── MovieClient.java              # Feign调用movies-service
│       │   ├── UserClient.java
│       │   └── ReviewClient.java
│       ├── controller/
│       │   ├── PortalIndexController.java
│       │   ├── PortalMovieListController.java
│       │   ├── PortalReviewController.java
│       │   └── PortalUserController.java
│       ├── entity/             # DTO实体类
│       └── config/
│           └── WebConfig.java
│
├── recommendation-service/     # 推荐服务
│   └── src/main/java/edu/cuit/recommendation/
│       ├── config/
│       │   └── RestTemplateConfig.java
│       ├── controller/
│       │   └── RecommendationController.java
│       └── service/
│           ├── MovieClientService.java         # 带熔断的HTTP客户端
│           └── RecommendationService.java
│
├── gateway-service/            # API网关
│   └── src/main/java/edu/cuit/gateway/
│       ├── GatewayApplication.java
│       └── filter/
│           ├── AuthFilter.java                 # JWT认证过滤器
│           └── LoggingFilter.java              # 请求日志过滤器
│
├── uploads/posters/            # 上传的海报文件
│
├── 微服务架构改造说明.md        # 本文档
├── start.sh                    # 一键启动（支持多实例）
├── stop.sh                     # 一键关闭
└── status.sh                   # 状态检查
```

---

## 十六、改造中遇到的关键问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| Config Server 中文路径报错 | Git URI 编码问题 | 改用 `native` 模式 |
| movies-service 数据库配置获取失败 | Config Server 未正确启动 | `application.properties` 中添加本地 fallback |
| movies-service 启动失败 | Security 依赖引入后缺少配置 | 添加 `SecurityConfig` 关闭 CSRF 并放行所有请求 |
| Resilience4j 熔断不生效 | `@CircuitBreaker` 在同类方法自调用上，AOP 无法拦截 | 将 HTTP 调用抽离到独立的 `MovieClientService` |
| Gateway CORS 报错 | `allowCredentials: true` 不能与 `allowedOrigins: "*"` 同时使用 | 改为 `allowCredentials: false` |
| JWT `userId` 类型不匹配 | Gateway 用 `String.class` 读取 Integer 类型的 claim | 改为 `claims.get("userId").toString()` |
| 新注册用户无法登录 | 默认 `permission=false` | MySQL 中手动 `UPDATE users SET permission=1` |
| 用户端侧边栏样式错乱 | 缺少 `.user-bar` CSS 样式 | 在 addreview.html 中补全用户栏样式 |
| 管理员端电影编辑日期未预填充 | HTML5 date 输入需要 `yyyy-MM-dd` 格式 | 使用 `th:value="${#dates.format(movie.releaseDate, 'yyyy-MM-dd')}"` |
| 电影海报外链失效 | 外部图片地址可能失效 | 添加 `onerror` 回退到占位图 + 支持本地上传 |
| 多实例登录状态丢失 | Session 存储在单个 JVM 内存中，Gateway 轮询到另一实例时无 Session | Spring Session Redis，所有实例共享同一 Session 存储 |
